# Browser-safe Supabase config (RLS-respecting). Provisioned per company.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-side Postgres connection string. NEVER expose to the browser.
DATABASE_URL=
